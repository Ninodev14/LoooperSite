import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Pour déployer sur Netlify, ajouter l'adapter Netlify si besoin
  // import netlify from '@astrojs/netlify';
  // adapter: netlify(),
});
